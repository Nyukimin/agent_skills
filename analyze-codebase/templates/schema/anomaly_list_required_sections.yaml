# 異常一覧（潜在バグ一覧）の必須セクション（H2 見出し）

required_sections:
  - "概要"
  - "潜在バグ（コード上の問題）"
  - "設計書との乖離"
  - "未実装機能"
  - "名前不一致・タイポ"
  - "関連ドキュメント"

# 概要ではこの一覧の対象と深刻度の考え方を2〜3行で記述すること。
# プロファイルで anomaly_categories を変更している場合はそれに合わせる。
